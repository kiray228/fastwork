# Разбор экранов EasyTap (часть 1: онбординг и главная)

> Источник — скриншоты обучающего тура приложения (7 шагов, получены 1–5).
> Копируем **структуру и механики**, визуальный стиль делаем свой.

---

## 1. Навигация: три вкладки

| Вкладка | Иконка | Назначение (из тура) |
|---|---|---|
| **Главная** | пламя | «Здесь публикуются подработки от компаний» |
| **Мои подработки** | портфель | «Здесь будут ваши подработки» |
| **Профиль** | человек | «Настройки аккаунта, информация о выплатах» |

> **Важное наблюдение:** отдельной вкладки «Кошелёк» нет. Выплаты живут
> **внутри профиля**. Это упрощает нам этап 5: кошелёк — не корневой раздел,
> а экран второго уровня.

Итого всего **три корневых раздела**. Для приложения с таким количеством
функций это мало — признак хорошо продуманной навигации.

---

## 2. Структура главного экрана

Сверху вниз:

1. **Логотип** по центру шапки
2. **Лента «историй»** — горизонтальный список круглых иконок с цветной
   обводкой (как в соцсетях). Судя по иллюстрациям — база знаний:
   выплаты, документы, санкнижка, чат, правила, договор
3. **Календарь-полоса** — горизонтальный выбор дня: сокращение дня недели
   сверху, число снизу
4. **Список карточек смен**
5. **Плавающие кнопки «Фильтр» и «Сортировка»** над таб-баром
6. **Плавающая кнопка чата поддержки** (круг, правый нижний угол)

### Календарь-полоса
- Выбранный день — залитый плашкой, текст белый
- Остальные — светлая плашка, тёмный текст
- Часть дней визуально приглушена (вероятно, недоступны или вне окна публикации)
- Горизонтальная прокрутка, видно ~7 дней

Это **ровно та механика, которую ты описал с самого начала**: открываешь
дату — видишь предложения на этот день.

---

## 3. Карточка смены

Состав, сверху вниз:

| Элемент | Пример | Заметки |
|---|---|---|
| Сумма | **12 100 ₸** | самый крупный элемент карточки |
| Логотип компании | справа вверху | квадрат со скруглением |
| Время | 🕐 10:00 - 22:00 | |
| Услуга | 💼 Услуги сотрудника склада | |
| Место | 📍 Золотое яблоко • г. Алматы, ул. Султана Бейбарыса, 1 | бренд + адрес |
| Действие | **Подробнее** | широкая кнопка |
| Поделиться | иконка справа от кнопки | |

**Состояние «мест нет»:** кнопка «Подробнее» заменяется на неактивную
серую «Мест нет». Карточка остаётся в списке, но взаимодействие закрыто.

> Подтверждает `workers_needed` из нашей модели: система считает свободные
> места и меняет состояние карточки.

---

## 4. Находка: неоплачиваемый перерыв

Проверим арифметику карточек:

| Сумма | Время | Часов | Сумма ÷ 12 ч | Сумма ÷ 11 ч |
|---|---|---|---|---|
| 12 100 ₸ | 10:00–22:00 | 12 | 1008,33 | **1100** |
| 7 700 ₸ | 10:00–22:00 | 12 | 641,67 | **700** |

Деление на 12 часов даёт некрасивые дроби, деление на **11** — ровные
ставки 1100 ₸ и 700 ₸ в час.

Вывод: **из смены вычитается один час неоплачиваемого перерыва.**

Значит в `shifts` нужна колонка `break_minutes`, а сумма считается так:

```
оплачиваемые минуты = (end_minutes − start_minutes) − break_minutes
сумма = оплачиваемые минуты / 60 × hourly_rate
```

> Это хороший пример того, зачем разбирать чужой продукт: такую деталь
> невозможно придумать, её можно только заметить. И заметна она оказалась
> именно потому, что мы решили хранить **ставку**, а не готовую сумму —
> иначе сверять было бы нечего.

---

## 5. Находка: формулировка «Услуги ...»

Не «Грузчик», не «Работник склада», а **«Услуги сотрудника склада»**.

Это не стилистика, а юридическая формулировка: платформа оформляет не
трудовые отношения, а **оказание услуг** самозанятым исполнителем. Отсюда
и отсутствие трудового договора, и выплаты на кошелёк вместо зарплаты.

Та самая «легальность», о которой ты спрашивал, — решается формулировками
и договором, а не кодом.

---

## 6. Что добавить в модель данных

| Изменение | Куда |
|---|---|
| `break_minutes INTEGER` | `shifts` — неоплачиваемый перерыв |
| `logo_path TEXT` | `companies` — логотип на карточке |
| `title TEXT` | `locations` — бренд точки («Золотое яблоко») |
| Таблица `stories` | лента базы знаний в шапке |
| Признак «день доступен» | вычисляется: есть ли published-смены на дату |

---

## 7. Экраны, которые предстоит спроектировать

Из тура известно назначение, но не содержимое:

- **Подробнее** (карточка смены целиком) — шаг 6–7 тура, ещё не получен
- **Мои подработки** — вероятно вкладки «активные / архив»
- **Профиль** — настройки, выплаты, документы, рейтинг
- Экраны фильтра и сортировки
- Регистрация и верификация

---

## 8. О визуальном стиле

Копируем **структуру экранов и механики**, не оформление:
компоновка карточки, календарь-полоса, три вкладки, состояние «мест нет» —
это функциональные решения, и они хорошие.

Логотип, название, фирменные цвета и тексты — чужая собственность, берём своё.
Палитру и типографику fastwork подберём отдельно.

---

# Часть 2: «Мои подработки», профиль, состояния

---

## 9. Экран «Мои подработки»

**Две вкладки-переключателя сверху: «В работе» / «Архив».**

Пустое состояние: иллюстрация + заголовок «Пока пусто…» + пояснение
«Здесь будут принятые подработки».

> **Это прямое подтверждение тезиса из первой версии модели.**
> Архив — не отдельная таблица, а **вкладка над той же таблицей**:
> «В работе» — `applications` со статусами `pending`/`accepted`/`in_progress`,
> «Архив» — `completed`/`rejected`/`cancelled_by_worker`/`no_show`.
> Один запрос, разный фильтр по `status`.

Формулировка «принятые подработки» подсказывает, что во вкладке «В работе»
показываются отклики, уже подтверждённые заказчиком.

---

## 10. Состояние «Мест нет» — полный текст

> «Подработку уже заняли другие пользователи.
> **Заходите чаще, место может освободиться**»

Важное уточнение механики: заполненная смена **не исчезает из ленты** и
может снова стать доступной. Значит отмена отклика исполнителем
(`cancelled_by_worker`) **освобождает место**, и счётчик мест обязан это
учитывать.

Отсюда следует, что свободные места нельзя считать как
«`workers_needed` − сколько всего откликнулись». Считать надо только
активные отклики:

```sql
SELECT COUNT(*) FROM applications
WHERE shift_id = ? AND status IN ('accepted', 'in_progress');
```

---

## 11. Экран «Профиль»

### Шапка
- Слева — колокольчик (уведомления), по центру логотип, справа — шестерёнка (настройки)
- Круглый аватар, под ним бейдж «щит × 3» (назначение точно не установлено —
  вероятно, уровень доверия или число подтверждённых документов)
- Имя пользователя
- Ряд плашек: **⭐ 4.0** · **Подработок - 0** · **💯 Новичок**
- Отдельная плашка: **✅ Верифицированы**

### Меню, группа 1
| Пункт | Правая часть |
|---|---|
| Выплаты | «Вознаграждение» |
| **Требуется подписание АВР** | |
| Пригласить друга | **+ 1 000 ₸** |
| Санитарная книжка | |
| Промокоды | |

### Меню, группа 2
| Пункт | Правая часть |
|---|---|
| Трудоустройство | **+ 5 000 ₸** |
| Частые вопросы | |
| EasyTap Premium | |
| Опыт работы | |
| Личные данные | |
| Город | **Шымкент** |

Внизу — ссылки на Instagram, TikTok, публикацию в Forbes и версия сборки.

---

## 12. Главная находка: АВР

**АВР — акт выполненных работ.** Пункт «Требуется подписание АВР» означает,
что приложение содержит **электронное подписание юридических документов**.

Связка полностью объясняет, как устроена «легальность»:

1. Услуга называется «Услуги сотрудника склада» — не трудовые отношения
2. Компания и платформа связаны **договором** (вне приложения)
3. После оказания услуги исполнитель подписывает **АВР** — акт
4. Только на основании подписанного акта производится **выплата**
5. Деньги называются «вознаграждение», а не «зарплата»

> Вот и ответ на исходный вопрос «чтобы всё было типо легально».
> Легальность — это **цепочка документов**, а приложение лишь фиксирует
> их статусы. Ни одна строчка Dart-кода легальности не создаёт.

Для модели данных это отдельная сущность `work_acts` со статусом
подписания, и жёсткое правило: **выплата невозможна без подписанного акта.**

---

## 13. Прочие находки

| Находка | Что означает для модели |
|---|---|
| **Город: Шымкент** | Лента фильтруется по городу → таблица `cities`, `users.city_id`, `locations.city_id` |
| **Рейтинг 4.0 при 0 подработок** | Рейтинг новичка — не NULL, а **стартовое значение** |
| **«Новичок»** | Уровень, вычисляемый из числа выполненных смен |
| **Подработок - 0** | Счётчик выполненных, считается `COUNT(*)` |
| **Пригласить друга +1 000 ₸** | Реферальная программа → `referrals` |
| **Трудоустройство +5 000 ₸** | Отдельная услуга: постоянное трудоустройство с бонусом |
| **Промокоды** | `promo_codes` + учёт применений |
| **EasyTap Premium** | Платная подписка — вторая модель монетизации помимо комиссии |
| **Опыт работы** | Анкета исполнителя |
| **Санитарная книжка** отдельным пунктом | Подтверждает `documents` |

### О стартовом рейтинге 4.0

В первой версии модели я заложил `rating NULL`, то есть «оценок ещё не
было». Прототип показывает новичку **4.0**.

Это продуктовое решение, а не техническое: у смен есть порог `min_rating`,
и при NULL новичок не прошёл бы ни один фильтр — то есть не смог бы начать
работать вообще. Стартовое значение решает проблему «холодного старта».

> Хороший пример того, что **значение по умолчанию — это бизнес-решение**.
> Технически можно и NULL, и 0, и 5.0 — но каждое меняет поведение продукта.

---

## 14. Что осталось неизвестным

- Экран «Подробнее» (карточка смены целиком) — так и не виден
- Содержимое фильтра и сортировки: по каким полям
- Экран выплат и вывода средств
- Процесс регистрации и верификации
- Что означает бейдж «щит × 3»

---

# Часть 3: фильтр, сортировка, ночные смены

---

## 15. Экран «Фильтр» (полноэкранное модальное окно)

Шапка: **✕** слева · заголовок «Фильтр» по центру · **«Очистить»** справа.

| Группа | Содержимое | Тип выбора |
|---|---|---|
| **Услуги** | чипы: «Ритейл», «Логистическая компания» + «Показать все» | множественный |
| **Компании** | чипы с логотипами: «Sinsay», «Заммлер Казахстан» + «Показать все» | множественный |
| — | переключатель **«Показать только открытые смены»** | вкл/выкл |

Внизу — широкая кнопка **«Показать результаты»**.

> **Чего в фильтре нет:** диапазона оплаты, времени, расстояния.
> Фильтр намеренно скромный — три параметра. Хороший пример того, что
> продуманный продукт не пытается дать пользователю всё сразу.

Переключатель «только открытые смены» — это фильтр по вычисляемому
значению (есть ли свободные места), а не по колонке. На больших объёмах
такие фильтры самые тяжёлые для БД.

---

## 16. Экран «Сортировка» (шторка снизу)

Одиночный выбор (радиокнопки):

- По расстоянию
- **Сначала утренние смены** (выбрано по умолчанию)
- Сначала вечерние смены

---

## 17. Находка: расстояние и геолокация

На карточках появился бейдж **«➤ 6.73 км»**, а в статус-баре — значок
активной геолокации.

Значит приложение:
1. Запрашивает координаты пользователя
2. Считает расстояние до `locations.lat/lon` каждой смены
3. Умеет сортировать по нему

Расстояние **не хранится в БД** — оно своё для каждого пользователя и
меняется при каждом шаге. Это вычисляемое значение в чистом виде.

> Для домена это означает, что `Shift` как сущность расстояния не содержит.
> Оно приходит отдельно — от источника геолокации — и вычисляется уже
> при подготовке данных для экрана.

---

## 18. Находка: теги на смене

У смены Zammler под адресом появились два чипа:

- **«Можно опоздать»**
- **«Вознаграждение завтра»**

Это признаки-приманки, вынесенные на карточку. Технически возможны два
подхода:

| Подход | Плюс | Минус |
|---|---|---|
| Булевы колонки `late_allowed`, `fast_payout` | просто, быстро фильтровать | каждый новый признак = миграция схемы |
| Таблицы `tags` + `shift_tags` (М:М) | новый тег добавляется строкой в БД | сложнее запрос |

Выбран второй: признаки такого рода имеют свойство размножаться, а менять
схему ради каждого — плохая практика.

---

## 19. Главная находка: смены через полночь

Вторая карточка Zammler: **12 100 ₸, время 18:00-06:00.**

Смена **заканчивается на следующий день**. Проверим нашу формулу:

```
start = 18:00 = 1080 минут
end   = 06:00 =  360 минут
длительность = 360 − 1080 = −720   ← отрицательная
```

**Формула из версии модели ломается.** Правильно так:

```
длительность = end_minutes > start_minutes
             ? end_minutes − start_minutes
             : (1440 − start_minutes) + end_minutes
```

Проверка: (1440 − 1080) + 360 = **720 минут = 12 часов**.
Минус час перерыва → 11 оплачиваемых часов → 12 100 / 11 = **1100 ₸/час**.

### Теория перерыва подтверждена на четырёх карточках

| Смена | Сумма | Время | Часов | −перерыв | Ставка |
|---|---|---|---|---|---|
| Золотое яблоко | 12 100 ₸ | 10:00–22:00 | 12 | 11 | **1100** |
| Zara | 7 700 ₸ | 10:00–22:00 | 12 | 11 | **700** |
| Zammler ПП | 11 000 ₸ | 10:00–21:00 | 11 | 10 | **1100** |
| Zammler Хаб | 12 100 ₸ | 18:00–06:00 | 12 | 11 | **1100** |

Четыре карточки, три разные длительности, включая ночную — и всюду ровные
ставки. Гипотеза подтверждена.

> Это и есть работа с реальными данными: одна ночная смена вскрыла ошибку
> в формуле **до того, как мы написали хоть строчку кода**. Найди мы её
> после запуска, она бы выглядела как «приложение показывает отрицательную
> оплату» — и искали бы её в интерфейсе, а не в арифметике.

---

## 20. Находка: приглушённые даты

В обеих сериях скриншотов **одни и те же дни (пн 21, вт 22) показаны
бледнее остальных**, хотя выбранная дата менялась.

Значит приглушение — не оформление и не позиция в списке, а **данные**:
в эти дни смен нет. Календарь-полоса должна знать, на каких датах есть
публикации.

Нужен отдельный запрос — количество смен по датам:

```sql
SELECT work_date, COUNT(*) FROM shifts
WHERE status = 'published' AND city_id = ?
GROUP BY work_date;
```

---

## 21. Служба поддержки — сторонний виджет

Экран чата («Сәлем, это служба поддержки 💜», кнопки «Чат» и «Отправить
сообщение», «Как правило, мы отвечаем в течение дня») — оформление
готового решения (похоже на Intercom), а не собственный экран приложения.

> **Вывод для нас:** поддержку они не разрабатывали. Для учебного проекта
> `support_tickets` мы всё же реализуем сами — именно ради практики с
> перепиской и связью 1-ко-многим.

Приветствие на казахском при русском интерфейсе — признак двуязычности
продукта.

---

## 22. Что нужно добавить в модель

| Изменение | Куда |
|---|---|
| Таблицы `tags` + `shift_tags` | признаки «можно опоздать», «вознаграждение завтра» |
| Исправленная формула длительности | правило расчёта оплаты |
| `crosses_midnight` как вычисляемый признак | не хранить, выводить из времени |
| Индекс `(city_id, work_date, status)` | главный запрос ленты |
| Расстояние | **не хранить** — вычисляется из геолокации |
